{
  "private": true,
  "scripts": {
    "build": "eslint src && spago build --censor-stats --strict --pedantic-packages",
    "test": "spago test --offline"
  },
  "devDependencies": {
    "eslint": "^9.12.0"
  }
}

package:
  name: now
  publish:
    license: MIT
    version: 6.0.0
    location:
      githubOwner: purescript-contrib
      githubRepo: purescript-now
  dependencies:
    - datetime
    - effect
    - prelude
  test:
    main: Test.Main
    dependencies:
      - assert
      - console
      - effect
      - either
      - exceptions
      - node-process
      - transformers
workspace:
  extraPackages: {}
  packageSet:
    registry: 60.5.0
